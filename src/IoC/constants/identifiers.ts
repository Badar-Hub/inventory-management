const SERVICE_IDENTIFIER = {
  RestaurantService: Symbol.for("RestaurantService"),
  ApiService: Symbol.for("ApiService"),
  ValuesService: Symbol.for("ValuesService")
};

export default SERVICE_IDENTIFIER;
